# Debug Docker Workflow

**When to use:** A container won't start, crashes on boot, or logs show errors.

1. Check running containers
2. Inspect logs
3. Verify config/env
4. Rebuild only if required
5. Avoid using --no-cache unless necessary